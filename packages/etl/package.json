{
  "name": "@ntp/etl",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --loader ts-node/esm src/index.ts",
    "ingest": "node --loader ts-node/esm src/index.ts",
    "lint": "echo 'No lint yet'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@ntp/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
